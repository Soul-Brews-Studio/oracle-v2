# PR #716 Review-Fix Summary

Date: 2026-04-12
PR: https://github.com/Soul-Brews-Studio/arra-oracle-v3/pull/716

## Fixed

- Closed `/register` when `MCP_AUTH_TOKEN` is unset so OAuth registration no longer falls open.
- Switched PIN throttling to the actual connection address (`c.env.remoteAddress`) so spoofed forwarding headers cannot bypass lockout.
- Added focused OAuth integration coverage for both fixes.
- Updated operator docs/config comments to reflect the enforced auth requirement.
- Updated the issue-6 revalidation report so it matches the branch contents and current validation state.

## Validation

- `bun test src/integration/oauth.test.ts` — PASS (`19 passed, 0 failed`)
- `bun run build` — PRE-EXISTING FAILURES remain in `src/server-legacy.ts` and `src/server/handlers.ts`

## Remaining Risk

- The repository still has pre-existing TypeScript build failures outside this change set. They were not introduced by this review-fix.
